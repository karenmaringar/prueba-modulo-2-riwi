# prueba-modulo-2-riwi
This Project is replica of a responsive landing page, The project is built using pure HTML and CSS, with no JavaScript.

Project Structure
The project is organized into the following folders:
It contains two folders:

styles.css

index.css


styles.css → Imports all styles

img/ – Folder where each HTML file stores its corresponding images.

